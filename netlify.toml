[build]
  command = "npm run build && npm run seed"
  functions = "netlify/functions"
  publish = "dist/demo"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"
